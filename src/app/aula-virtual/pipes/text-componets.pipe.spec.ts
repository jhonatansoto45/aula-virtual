import { TextComponetsPipe } from './text-componets.pipe';

describe('TextComponetsPipe', () => {
  it('create an instance', () => {
    const pipe = new TextComponetsPipe();
    expect(pipe).toBeTruthy();
  });
});
